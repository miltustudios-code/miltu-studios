# Miltu Studios — site with working Resume-to-Interview OS demo

Upload BOTH of these to the existing `miltu-studios` GitHub repository:

- `index.html` in the repository root
- the entire `resume-to-interview-os` folder, containing its own `index.html`

Do not upload the ZIP itself.

Once GitHub Pages updates, the links should be:

Main site:
https://miltustudios-code.github.io/miltu-studios/

Resume-to-Interview OS:
https://miltustudios-code.github.io/miltu-studios/resume-to-interview-os/

The main site's "View live demo" button now uses a relative link, so it will open the demo from the same site.
